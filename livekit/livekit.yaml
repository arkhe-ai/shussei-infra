port: 7880
rtc:
  tcp_port: 7881
  use_external_ip: true
keys:
  ${LIVEKIT_API_KEY}: ${LIVEKIT_API_SECRET}
turn:
  enabled: false
redis:
  address: redis:6379
  username: default
  password: ${REDIS_PASSWORD}
